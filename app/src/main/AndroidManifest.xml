<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".FinancialTrackApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinancialTrack"
        tools:targetApi="31">
        <activity
            android:name=".ui.reports.ReportsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.reports.Reports"
            android:exported="false" />
        <activity
            android:name=".ui.profile.FinancialGoals"
            android:exported="false" />
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false" /> <!-- LoginActivity is the starting point of the app -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- RegisterActivity for user sign-up -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" /> <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Notification Activity -->
        <activity
            android:name=".ui.notifications.NotificationActivity"
            android:exported="false" /> <!-- Debt & Loan Activity -->
        <activity
            android:name=".ui.debt.DebtActivity"
            android:exported="false" />
        <activity
            android:name=".ui.transaction.TransactionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.accounts.AccountsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.budget.BudgetActivity"
            android:exported="false" />
    </application>

</manifest>
